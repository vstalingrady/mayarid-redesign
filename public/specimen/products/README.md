# Product photos

- `items/{product-id}.jpg` — **real** Unsplash photographs (Unsplash License), one unique shot per product service.
- Root `*.jpg` family files — older lifestyle pack (kept as fallback for directions lab).

Hover/cycle in the product explorer uses `items/` via `PRODUCT_IMAGES` in `src/data/productTypes.ts`.
